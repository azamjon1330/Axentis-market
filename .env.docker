# ============================================================================
# Azaton Frontend - Docker Production Environment
# ============================================================================
# Этот файл используется в Docker контейнере на сервере
# Nginx проксирует /api и /uploads на backend:3000

# API Backend URL (через nginx proxy)
VITE_API_URL=/api

# Socket.io URL (через nginx proxy)
VITE_SOCKET_URL=
