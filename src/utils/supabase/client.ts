// STUB файл - Supabase больше не используется
// Функции оставлены только для старого кода, возвращают null

export function getSupabaseClient() {
  console.warn('⚠️ getSupabaseClient() вызвана, но Supabase больше не используется');
  return null as any;
}

// 🔄 Экспорт для обратной совместимости
export const supabase = null as any;